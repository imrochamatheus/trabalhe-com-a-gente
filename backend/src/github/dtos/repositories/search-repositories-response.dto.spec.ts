import { SearchRepositoriesResponseDto } from "./search-repositories-response.dto";

describe("SearchRepositoriesResponseDto", () => {
  it("should be defined", () => {
    expect(new SearchRepositoriesResponseDto()).toBeDefined();
  });
});
